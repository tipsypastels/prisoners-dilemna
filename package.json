{
  "name": "prisoners-dilemna",
  "private": true,
  "workspaces": [
    "js/core",
    "js/editor"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:editor",
    "build:core": "cd js/core && npm run build",
    "build:editor": "cd js/editor && npm run build"
  },
  "devDependencies": {
    "rolldown": "1.0.0-beta.1",
    "tailwindcss": "^3.4.17"
  }
}
