{
  "name": "prisoners-dilemna",
  "private": true,
  "workspaces": [
    "js/core",
    "js/editor"
  ],
  "scripts": {
    "build": "concurrently \"npm run build:core\" \"npm run build:editor\"",
    "build:core": "cd js/core && npm run build",
    "build:editor": "cd js/editor && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rolldown": "1.0.0-beta.1",
    "tailwindcss": "^3.4.17"
  }
}
